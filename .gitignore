# Environment secrets — never commit
.env
.env.local
.env.prod

# Python
__pycache__/
*.py[codz]
*.so
.Python
*.egg-info/
dist/
build/

# HuggingFace cache
.cache/

# Postgres data dir (Docker volume — never commit)
data/audit-db/
